[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm ci",
  "pip install bitcoinlib base58 --break-system-packages"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
